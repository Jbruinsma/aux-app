package com.aux.entity;

// TODO: map the `user_playlists_added_to` table (see backend/models.py UserPlaylistAddedTo), then annotate with @Entity/@Table.
// Columns: (user_id, playlist_uuid) composite PK, owner_id -> users.id
public class UserPlaylistAddedToEntity {
}
