package com.aux.entity;

// TODO: map the `playlists` table (see backend/models.py PlaylistModel), then annotate with @Entity/@Table.
// Columns: uuid (PK), owner_id -> users.id, name, cover, is_public (INTEGER 0/1)
public class PlaylistEntity {
}
