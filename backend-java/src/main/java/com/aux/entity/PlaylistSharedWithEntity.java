package com.aux.entity;

// TODO: map the `playlist_shared_with` table (see backend/models.py PlaylistSharedWith), then annotate with @Entity/@Table.
// Columns: id (INTEGER PK autoincrement), playlist_uuid, user_id, can_edit (INTEGER 0/1), position
public class PlaylistSharedWithEntity {
}
