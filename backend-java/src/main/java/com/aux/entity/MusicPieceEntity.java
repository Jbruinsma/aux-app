package com.aux.entity;

// TODO: map the `music_pieces` table (see backend/models.py MusicPieceModel), then annotate with @Entity/@Table.
// Columns: uuid (PK), playlist_uuid -> playlists.uuid, position, mp3_file, cover, name, artist, is_favorite (INTEGER 0/1)
public class MusicPieceEntity {
}
