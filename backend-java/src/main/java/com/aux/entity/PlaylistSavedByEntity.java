package com.aux.entity;

// TODO: map the `playlist_saved_by` table (see backend/models.py PlaylistSavedBy), then annotate with @Entity/@Table.
// Columns: id (INTEGER PK autoincrement), playlist_uuid, user_id, position
public class PlaylistSavedByEntity {
}
