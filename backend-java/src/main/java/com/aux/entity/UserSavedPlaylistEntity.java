package com.aux.entity;

// TODO: map the `user_saved_playlists` table (see backend/models.py UserSavedPlaylist), then annotate with @Entity/@Table.
// Columns: (user_id, playlist_uuid) composite PK, owner_id -> users.id
public class UserSavedPlaylistEntity {
}
